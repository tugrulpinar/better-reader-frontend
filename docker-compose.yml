services:
  app:
    build: .
    ports:
      - "2018:2018"
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/node_modules
    command: yarn dev
    container_name: better-reader-frontend
